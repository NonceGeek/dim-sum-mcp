{
  "name": "dimsum-mcp-bridge",
  "version": "1.0.0",
  "description": "HTTP to stdio MCP proxy bridge for AI Dimsum MCP Server",
  "main": "mcp-bridge.js",
  "bin": {
    "dimsum-mcp-bridge": "./mcp-bridge.js"
  },
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "mcp",
    "dimsum",
    "cantonese",
    "proxy",
    "bridge",
    "http",
    "stdio"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/dimsum-mcp-workers.git"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260214.0"
  }
}
